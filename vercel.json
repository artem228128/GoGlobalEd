{
  "buildCommand": "git lfs pull --include=\"*.mp4\" --exclude=\"\" && npm run build",
  "installCommand": "git lfs install && npm install",
  "framework": "nextjs",
  "functions": {
    "src/app/**/*.tsx": {
      "maxDuration": 30
    }
  }
} 